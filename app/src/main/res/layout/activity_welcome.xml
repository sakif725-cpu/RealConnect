<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0F172A"
    android:padding="28dp">

    <!-- Center Container -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <!-- App Logo Card -->
        <com.google.android.material.card.MaterialCardView
            android:layout_width="96dp"
            android:layout_height="96dp"
            app:cardBackgroundColor="#1E293B"
            app:cardCornerRadius="32dp"
            app:cardElevation="12dp"
            app:strokeColor="#38BDF8"
            app:strokeWidth="2dp">

            <ImageView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:padding="22dp"
                android:src="@drawable/ic_call"
                app:tint="#38BDF8" />
        </com.google.android.material.card.MaterialCardView>

        <!-- Brand Name -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="28dp"
            android:text="RealConnect"
            android:textColor="#38BDF8"
            android:textSize="20sp"
            android:textStyle="bold"
            android:letterSpacing="0.08" />

        <!-- Greeting -->
        <TextView
            android:id="@+id/text_greeting"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:gravity="center"
            android:text="Welcome!"
            android:textColor="@color/white"
            android:textSize="32sp"
            android:fontFamily="sans-serif-medium" />

        <!-- Subtitle Description -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:gravity="center"
            android:paddingHorizontal="16dp"
            android:text="Secure AI-guarded voice calling and real-time messaging."
            android:textColor="#94A3B8"
            android:textSize="15sp"
            android:lineSpacingExtra="4dp" />

        <!-- Arrow Button Under Greeting (Starts App) -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/btn_start_app"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="48dp"
            android:contentDescription="Start App"
            android:src="@drawable/ic_arrow_forward"
            app:backgroundTint="#0EA5E9"
            app:elevation="8dp"
            app:fabCustomSize="64dp"
            app:maxImageSize="30dp"
            app:rippleColor="#38BDF8"
            app:tint="@color/white" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="Tap arrow to continue"
            android:textColor="#64748B"
            android:textSize="13sp" />

    </LinearLayout>

    <!-- Footer Security Note -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp"
        android:text="🛡️ AI Spam &amp; Deepfake Defense Protected"
        android:textColor="#475569"
        android:textSize="12sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>